-- Сохранение полной истории ответов по каждому вопросу (вопрос, ответ пользователя, правильный ответ, объяснение).
-- Обязательно выполнить на БД, иначе страница «Результаты» попытки и сохранение деталей не будут работать.

ALTER TABLE test_attempts
ADD COLUMN IF NOT EXISTS results TEXT;
